# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: cross-module/cross-module-ui-flows.spec.ts >> Cross-Module UI Flow Audits @sales @purchase @smoke @full >> Purchase UI: Approved bill reflects outstanding balance in vendor profile
- Location: tests/cross-module/cross-module-ui-flows.spec.ts:79:9

# Error details

```
Error: Bill Discovery Failed: No vendors found in current company.
```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - generic [ref=e5]:
    - generic [ref=e7]:
      - generic [ref=e8]:
        - img "BM Tech" [ref=e10]: BT
        - generic [ref=e11]:
          - button "BM Tech" [ref=e12] [cursor=pointer]:
            - generic: BM Tech
            - img [ref=e14]
          - generic [ref=e16] [cursor=pointer]:
            - button "Company Detail" [ref=e17]:
              - img [ref=e18]
            - button "Edit Company" [ref=e21]:
              - img [ref=e22]
            - button "Company Detail" [ref=e25]:
              - img [ref=e26]
      - generic [ref=e29]:
        - button "New" [ref=e30] [cursor=pointer]:
          - text: New
          - img [ref=e32]
        - img "Notifications" [ref=e37] [cursor=pointer]
        - button "EC" [ref=e40] [cursor=pointer]:
          - img [ref=e41]
          - paragraph [ref=e43]: EC
        - button [ref=e44] [cursor=pointer]:
          - img [ref=e45]
        - generic [ref=e48] [cursor=pointer]:
          - img "System" [ref=e50]: S
          - generic [ref=e51]:
            - generic [ref=e52]: System
            - paragraph [ref=e53]: IT Administrator / User Manager
    - generic [ref=e55]:
      - generic [ref=e57]:
        - generic [ref=e58]:
          - img "BM Tech" [ref=e61]: BT
          - paragraph [ref=e62]: Welcome, System
        - paragraph [ref=e64]: From meticulous bookkeeping to seamless inventory control, we've got your back.
        - generic [ref=e65]:
          - link "Dashboard" [ref=e66] [cursor=pointer]:
            - /url: /dashboard
          - link "Settings" [ref=e67] [cursor=pointer]:
            - /url: /settings/company/details
        - generic [ref=e68]:
          - link "Add Customer" [ref=e69] [cursor=pointer]:
            - /url: /receivables/customers/new
            - img [ref=e72]
            - text: Add Customer
          - link "Add Invoice" [ref=e73] [cursor=pointer]:
            - /url: /receivables/invoices/new
            - img [ref=e76]
            - text: Add Invoice
          - link "Add Receipt" [ref=e77] [cursor=pointer]:
            - /url: /receivables/receipts/new
            - img [ref=e80]
            - text: Add Receipt
          - link "Add Sales Order" [ref=e81] [cursor=pointer]:
            - /url: /receivables/sale-orders/new
            - img [ref=e84]
            - text: Add Sales Order
        - paragraph [ref=e86]: Quick Access
        - generic [ref=e87]:
          - generic [ref=e88]:
            - link "Sales Sales" [ref=e90] [cursor=pointer]:
              - /url: /receivables/overview/
              - button "Sales Sales" [ref=e91]:
                - generic [ref=e92]:
                  - img "Sales" [ref=e93]
                  - paragraph [ref=e94]: Sales
            - link "Purchase Purchase" [ref=e96] [cursor=pointer]:
              - /url: /payables/overview/
              - button "Purchase Purchase" [ref=e97]:
                - generic [ref=e98]:
                  - img "Purchase" [ref=e99]
                  - paragraph [ref=e100]: Purchase
            - link "Accounting Accounting" [ref=e102] [cursor=pointer]:
              - /url: /accounting/overview
              - button "Accounting Accounting" [ref=e103]:
                - generic [ref=e104]:
                  - img "Accounting" [ref=e105]
                  - paragraph [ref=e106]: Accounting
            - link "Leases Leases" [ref=e108] [cursor=pointer]:
              - /url: /leases/leases/?page=1&pageSize=15
              - button "Leases Leases" [ref=e109]:
                - generic [ref=e110]:
                  - img "Leases" [ref=e111]
                  - paragraph [ref=e112]: Leases
            - link "Assets Assets" [ref=e114] [cursor=pointer]:
              - /url: /assets/overview
              - button "Assets Assets" [ref=e115]:
                - generic [ref=e116]:
                  - img "Assets" [ref=e117]
                  - paragraph [ref=e118]: Assets
            - link "Budgets Budgets" [ref=e120] [cursor=pointer]:
              - /url: /public-sector-budgets/overview
              - button "Budgets Budgets" [ref=e121]:
                - generic [ref=e122]:
                  - img "Budgets" [ref=e123]
                  - paragraph [ref=e124]: Budgets
            - link "Payroll Payroll" [ref=e126] [cursor=pointer]:
              - /url: /payrolls
              - button "Payroll Payroll" [ref=e127]:
                - generic [ref=e128]:
                  - img "Payroll" [ref=e129]
                  - paragraph [ref=e130]: Payroll
            - link "Report Report" [ref=e132] [cursor=pointer]:
              - /url: /reports
              - button "Report Report" [ref=e133]:
                - generic [ref=e134]:
                  - img "Report" [ref=e135]
                  - paragraph [ref=e136]: Report
          - button "View All" [ref=e137] [cursor=pointer]:
            - text: View All
            - img [ref=e139]
      - img "Floating Icon" [ref=e142]
  - generic:
    - region "Notifications-top"
    - region "Notifications-top-left"
    - region "Notifications-top-right"
    - region "Notifications-bottom-left"
    - region "Notifications-bottom"
    - region "Notifications-bottom-right"
  - generic:
    - region "Notifications-top"
    - region "Notifications-top-left"
    - region "Notifications-top-right"
    - region "Notifications-bottom-left"
    - region "Notifications-bottom"
    - region "Notifications-bottom-right"
```

# Test source

```ts
  143 |     const period = process.env.BEFFA_PERIOD || 'yearly';
  144 |     const calendar = process.env.BEFFA_CALENDAR || 'ec';
  145 |     const params = `year=${year}&period=${period}&calendar=${calendar}`;
  146 |     const headers = { 'x-company': company, 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' };
  147 | 
  148 |     const safeJson = async (resp: any, label: string) => {
  149 |       const text = await resp.text();
  150 |       if (!resp.ok()) throw new Error(`${label} HTTP ${resp.status()}: ${text.substring(0, 200)}`);
  151 |       try { return JSON.parse(text); } catch (e) { throw new Error(`${label} invalid JSON: ${text.substring(0, 150)}`); }
  152 |     };
  153 | 
  154 |     // 1. Discover Vendor
  155 |     let resolvedVendorId = vendorId;
  156 |     if (!resolvedVendorId) {
  157 |       const vendorResp = await this.page.request.get(`${apiBase}/vendors?page=1&pageSize=10`, { headers });
  158 |       const vendorData = await safeJson(vendorResp, 'Vendor Discovery');
  159 |       const vendor = vendorData.items?.[0] || vendorData.data?.[0];
  160 |       if (!vendor) throw new Error('PO Discovery Failed: No vendors found.');
  161 |       resolvedVendorId = vendor.id;
  162 |     }
  163 | 
  164 |     // 2. Discover Accounts (AP + GL)
  165 |     const acctResp = await this.page.request.get(`${apiBase}/accounts?page=1&pageSize=50`, { headers });
  166 |     const acctData = await safeJson(acctResp, 'Accounts Discovery');
  167 |     const allAccounts = acctData.items || acctData.data || [];
  168 |     const apAccount = allAccounts.find((a: any) => a.account_type?.toLowerCase().includes('payable')) || allAccounts[0];
  169 |     const glAccount = allAccounts.find((a: any) => a.account_type?.toLowerCase().includes('expense')) || allAccounts[1] || allAccounts[0];
  170 | 
  171 |     // 4. Discover Locations if missing
  172 |     let locationId = itemData.locationId;
  173 |     let warehouseId = itemData.warehouseId;
  174 |     if (!locationId || !warehouseId) {
  175 |       const locResp = await this.page.request.get(`${apiBase}/locations?page=1&pageSize=10`, { headers });
  176 |       const locData = await safeJson(locResp, 'Location Discovery');
  177 |       const firstLoc = (locData.items || locData.data || [])[0];
  178 |       if (firstLoc) {
  179 |         locationId = firstLoc.id;
  180 |         warehouseId = firstLoc.warehouse_id || firstLoc.warehouse?.id;
  181 |       }
  182 |     }
  183 |     // 5. Discover Currency if missing
  184 |     const currResp = await this.page.request.get(`${apiBase}/currency?page=1&pageSize=5`, { headers });
  185 |     const currData = await safeJson(currResp, 'Currency Discovery');
  186 |     const currency = (currData.items || currData.data || [])[0];
  187 | 
  188 |     const payload = {
  189 |       accounts_payable_id: apAccount?.id,
  190 |       currency_id: currency?.id,
  191 |       po_date: new Date().toISOString().split('T')[0] + 'T00:00:00Z',
  192 |       po_items: [{
  193 |         item_id: itemData.itemId,
  194 |         general_ledger_account_id: glAccount?.id,
  195 |         location_id: locationId,
  196 |         quantity: qty,
  197 |         tax_id: itemData.taxId || null,
  198 |         unit_price: unitPrice,
  199 |         warehouse_id: warehouseId,
  200 |         description: `Purchase of ${itemData.itemName}`
  201 |       }],
  202 |       purchase_type_id: 4,
  203 |       vendor_id: resolvedVendorId
  204 |     };
  205 | 
  206 |     const response = await this.safePost(`${apiBase}/purchase-orders?year=${year}&period=${period}&calendar=${calendar}`, {
  207 |       data: payload,
  208 |       headers,
  209 |       label: 'Create Purchase Order'
  210 |     });
  211 | 
  212 |     if (!response.ok()) throw new Error(`PO API Creation Failed: ${response.status()} - ${await response.text()}`);
  213 |     const json = await response.json();
  214 |     return { success: true, poNumber: json.po_number, poId: json.id };
  215 |   }
  216 | 
  217 |   async createBillAPI(params: { itemData?: Record<string, any>; itemId?: string; quantity?: number; qty?: number; unitPrice?: number; vendorId?: string | null; apAccountId?: string | null; glAccountId?: string | null; discount_amount?: number; description?: string; poId?: string } = {}): Promise<{ success: boolean; ref: string; id: string; error?: string }> {
  218 |     const { itemData = {}, itemId = null, quantity = 10, qty = 10, unitPrice = 5000, vendorId = null, apAccountId = null, glAccountId = undefined, discount_amount = 0, description = null, poId = null } = params;
  219 |     const finalQty = quantity || qty;
  220 |     let apiBase = (process.env.API_URL || process.env.BASE_URL || 'http://localhost:8001').replace(/['"+]+/g, '').replace(/\/$/, '').replace(/:4173/, ':8001'); if (!apiBase.startsWith('http')) apiBase = 'http://' + apiBase;
  221 |     if (!apiBase.endsWith('/api')) apiBase += '/api';
  222 |     const token = await this._getAuthToken();
  223 |     const company = process.env.BEFFA_COMPANY as string;
  224 |     const year = process.env.BEFFA_YEAR || '2018';
  225 |     const period = process.env.BEFFA_PERIOD || 'yearly';
  226 |     const calendar = process.env.BEFFA_CALENDAR || 'ec';
  227 |     const qs = `year=${year}&period=${period}&calendar=${calendar}`;
  228 |     const headers = { 'x-company': company, 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' };
  229 | 
  230 |     const safeJson = async (resp: any, label: string) => {
  231 |       const text = await resp.text();
  232 |       if (!resp.ok()) throw new Error(`${label} HTTP ${resp.status()}: ${text.substring(0, 200)}`);
  233 |       try { return JSON.parse(text); } catch (e) { throw new Error(`${label} invalid JSON: ${text.substring(0, 150)}`); }
  234 |     };
  235 | 
  236 |     // 1. Discover Vendor - always use company-scoped discovery (never trust hardcoded UUIDs)
  237 |     let resolvedVendorId = vendorId;
  238 |     if (!resolvedVendorId) {
  239 |       // Use same pattern as createPurchaseOrderAPI which works reliably
  240 |       const vendorResp = await this.page.request.get(`${apiBase}/vendors?page=1&pageSize=10`, { headers });
  241 |       const vendorData = await safeJson(vendorResp, 'Vendor Discovery');
  242 |       const vendor = vendorData.items?.[0] || vendorData.data?.[0];
> 243 |       if (!vendor) throw new Error('Bill Discovery Failed: No vendors found in current company.');
      |                          ^ Error: Bill Discovery Failed: No vendors found in current company.
  244 |       resolvedVendorId = vendor.id;
  245 |     }
  246 | 
  247 |     // 2. Discover Accounts (AP + GL)
  248 |     const acctResp = await this.page.request.get(`${apiBase}/accounts?page=1&pageSize=50&${qs}`, { headers });
  249 |     const acctData = await safeJson(acctResp, 'Accounts Discovery');
  250 |     const allAccounts = acctData.items || acctData.data || [];
  251 | 
  252 |     // Improved strict AP discovery
  253 |     const discoveredAp =
  254 |       allAccounts.find((a: any) => a.name?.toLowerCase().includes('accounts payable')) ||
  255 |       allAccounts.find((a: any) => a.account_type?.toLowerCase().includes('payable')) ||
  256 |       allAccounts[0];
  257 | 
  258 |     const resolvedGlAccount = glAccountId !== undefined ? { id: glAccountId } : (allAccounts.find((a: any) => a.account_type?.toLowerCase().includes('expense')) || allAccounts[1] || allAccounts[0]);
  259 | 
  260 |     // 3. Discover Currency
  261 |     const currResp = await this.page.request.get(`${apiBase}/currency?${qs}`, { headers });
  262 |     const currData = await safeJson(currResp, 'Currency Discovery');
  263 |     const currency = currData.items?.[0] || currData.data?.[0];
  264 | 
  265 |     // 4. Discover Locations if missing
  266 |     let locationId = itemData.locationId;
  267 |     let warehouseId = itemData.warehouseId;
  268 |     if (!locationId || !warehouseId) {
  269 |       const locResp = await this.page.request.get(`${apiBase}/locations?page=1&pageSize=10&${qs}`, { headers });
  270 |       const locData = await safeJson(locResp, 'Location Discovery');
  271 |       const firstLoc = (locData.items || locData.data || [])[0];
  272 |       if (firstLoc) {
  273 |         locationId = firstLoc.id;
  274 |         warehouseId = firstLoc.warehouse_id || firstLoc.warehouse?.id;
  275 |       }
  276 |     }
  277 | 
  278 |     const payload = {
  279 |       accounts_payable_id: apAccountId || discoveredAp?.id,
  280 |       currency_id: currency?.id,
  281 |       invoice_date: new Date().toISOString().split('T')[0] + 'T00:00:00Z',
  282 |       due_date: new Date().toISOString().split('T')[0] + 'T00:00:00Z',
  283 |       items: [{
  284 |         item_id: itemData.itemId || itemData.id,
  285 |         general_ledger_account_id: resolvedGlAccount?.id || null,
  286 |         location_id: locationId,
  287 |         quantity: finalQty,
  288 |         tax_id: itemData.taxId || null,
  289 |         unit_price: unitPrice,
  290 |         warehouse_id: warehouseId,
  291 |         description: description || `Audit Bill of ${itemData.itemName || itemData.name}`,
  292 |         amount: finalQty * unitPrice,
  293 |         discount_amount: discount_amount
  294 |       }],
  295 |       vendor_id: resolvedVendorId,
  296 |       status: 'draft'
  297 |     };
  298 | 
  299 |     const response = await this.safePost(`${apiBase}/bills?${qs}`, {
  300 |       data: payload,
  301 |       headers,
  302 |       label: 'Create Bill'
  303 |     });
  304 | 
  305 |     if (!response.ok()) throw new Error(`Bill API Creation Failed: ${response.status()} - ${await response.text()}`);
  306 |     const json = await response.json();
  307 |     return { success: true, ref: json.invoice_number, id: json.id };
  308 |   }
  309 |   async createBillFromPoAPI(poId: string): Promise<{ success: boolean; billNumber: string; billId: string }> {
  310 |     let apiBase = (process.env.API_URL || process.env.BASE_URL || 'http://localhost:8001').replace(/['"+]+/g, '').replace(/\/$/, '').replace(/:4173/, ':8001'); if (!apiBase.startsWith('http')) apiBase = 'http://' + apiBase;
  311 |     if (!apiBase.endsWith('/api')) apiBase += '/api';
  312 |     const token = await this._getAuthToken();
  313 |     const company = process.env.BEFFA_COMPANY as string;
  314 |     const year = process.env.BEFFA_YEAR || '2018';
  315 |     const period = process.env.BEFFA_PERIOD || 'yearly';
  316 |     const calendar = process.env.BEFFA_CALENDAR || 'ec';
  317 |     const params = `year=${year}&period=${period}&calendar=${calendar}`;
  318 |     const headers = { 'x-company': company, 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' };
  319 | 
  320 |     const safeJson = async (resp: any, label: string) => {
  321 |       const text = await resp.text();
  322 |       if (!resp.ok()) throw new Error(`${label} HTTP ${resp.status()}: ${text.substring(0, 200)}`);
  323 |       try { return JSON.parse(text); } catch (e) { throw new Error(`${label} invalid JSON: ${text.substring(0, 150)}`); }
  324 |     };
  325 | 
  326 |     // 1. Fetch the Purchase Order to gather its precise mapping metadata
  327 |     const poResp = await this.page.request.get(`${apiBase}/purchase-order/${poId}?${params}`, { headers });
  328 |     const poData = await safeJson(poResp, `Fetch PO ${poId}`);
  329 | 
  330 |     // 2. Discover Accounts Payable ID for validation overlay
  331 |     const acctResp = await this.page.request.get(`${apiBase}/accounts?page=1&pageSize=50&${params}`, { headers });
  332 |     const acctData = await safeJson(acctResp, 'Accounts Discovery');
  333 |     const allAccounts = acctData.items || acctData.data || [];
  334 |     const apAccount = allAccounts.find((a: any) => a.account_type?.toLowerCase().includes('payable')) || allAccounts[0];
  335 | 
  336 |     // 3. Map strictly into `received_purchase_order_items`
  337 |     const receivedItems = (poData.po_items || []).map((item: any) => ({
  338 |       po_item_id: item.id,
  339 |       received_quantity: item.quantity,
  340 |       received_unit_price: item.unit_price
  341 |     }));
  342 | 
  343 |     if (receivedItems.length === 0) throw new Error(`PO ${poId} lacks interactable line-items.`);
```