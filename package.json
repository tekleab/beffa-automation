{
  "name": "beffa-automation",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "npm run test:unit && npx playwright test --project=Inventory --project=Purchases --project=Sales",
    "test:unit": "vitest run",
    "test:ui": "vitest --ui",
    "test:smoke": "npx playwright test --project=Inventory --project=Purchases --project=Sales --grep @smoke",
    "test:regression": "npx playwright test --project=Inventory --project=Purchases --project=Sales --grep @regression",
    "test:full": "npx playwright test --project=Inventory --project=Purchases --project=Sales",
    "test:ordered": "npm run clean && npx playwright test --project=Inventory && npx playwright test --project=Purchases && npx playwright test --project=Sales",
    "clean": "rm -rf test-results/ allure-results/ recordings/ artifacts/ coverage/",
    "allure:generate": "npx allure generate allure-results --clean -o allure-report && npx ts-node scripts/latency-tracker.ts",
    "latency:track": "npx ts-node scripts/latency-tracker.ts",
    "report:allure": "npm run allure:generate && npx allure open allure-report"
  },
  "keywords": [],
  "author": "Tekleab",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.6.0",
    "@vitest/ui": "^1.6.1",
    "allure-commandline": "^2.38.1",
    "allure-playwright": "^3.6.0",
    "allure-vitest": "^3.7.1",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.2",
    "vitest": "^1.6.1"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
